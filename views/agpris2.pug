doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js')
    script.
      $(document).ready(function(){
        $.ajax({
          url: 'http://agile.teaminnovationllc.com:3000/api/agileframeworks/', 
          success: function(r){
            var result = '<ul>';
            for( const f in r.AFs ) {
              result = result + '<li>' + r.AFs[f] + '</li>';
            }
            result = result + '</ul>';
            $("#div1").html(result);
          }
        });
      });
  body
    #div1
      h2 Let jQuery AJAX Change This Text  
       

      

