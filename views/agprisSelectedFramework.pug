extends layout

block content
  h1 #{title} for #{f} the #{framework} framework
  each f in frameworks
    a.menu(href='/agileframeworks/' + f) #{f}
  table
      each pv in principlesArray
        tr
          td.type #{pv.type}
          td.text #{pv.id}: #{pv.text}
      
      
      