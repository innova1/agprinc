extends layout

block content
  h1= title for #{f} framework
  each f in frameworks
    //if f==framework
    a.menu(href='/agileframeworks/' + f) #{f}
  table
      each pv in principlesArray
        tr
          td.type #{pv.type}
          td.text #{pv.text}
      
      
      